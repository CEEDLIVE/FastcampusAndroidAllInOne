<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ceed.live.android.kotlin.fastcampus.instagram">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MasterApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastcampusInstagram"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".post.OutstagramPostListActivity"
            android:exported="true" />
        <activity
            android:name=".member.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".permisson.PermissionActivity"
            android:exported="true" />
        <activity
            android:name=".http.RetrofitActivity"
            android:exported="true" />
        <activity
            android:name=".http.NetworkActivity"
            android:exported="true" />
        <activity
            android:name=".member.EmailSignUpActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>